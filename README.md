# Try-Frist
第一次用，看看会发生什么。

## 俄罗斯方块（Python 可视化）
仓库新增了一个基于 `tkinter` 的俄罗斯方块小游戏：`tetris.py`。

### 运行方式
```bash
python3 tetris.py
```

### 操作说明
- `←` / `→`：左右移动
- `↑`：旋转方块
- `↓`：加速下落
- `空格`：直接落地
- `R`：重新开始
